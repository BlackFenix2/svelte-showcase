{
  "private": true,
  "name": "portfolio",
  "description": "TODO",
  "version": "0.0.1",
  "scripts": {
    "lint": "stylelint **/*.svelte --fix && eslint . --ext .ts,.svelte --fix",
    "dev": "sapper dev",
    "build": "sapper build",
    "export": "sapper export",
    "start": "node __sapper__/build",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test": "run-p --race dev cy:run"
  },
  "dependencies": {
    "compression": "^1.7.1",
    "polka": "next",
    "sirv": "^1.0.6"
  },
  "devDependencies": {
    "sapper": "^0.28.0",
    "svelte": "^3.24.1",
    "svelte-components": "*"
  }
}
